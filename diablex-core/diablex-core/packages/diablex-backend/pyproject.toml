[build-system]
requires = ["setuptools>=68","wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "diablex-backend"
version = "0.0.0"
description = "Backend engine for Diablex (scaffold placeholder)."
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name="Diablex Team"}]
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["diablex_backend*"]
